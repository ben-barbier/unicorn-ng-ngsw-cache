import { PairValidatorDirective } from './pair-validator.directive';

describe('PairValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PairValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
